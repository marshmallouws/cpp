// Compile program:
// g++ program.cpp -o program.ex

// initialize array at runtime
// int *arr = new int[n];               --> Pointer to memory with n uninitialised integers

// Deallocate the array memory when not needed anymore
// delete [] b ;
